METASOURCES = AUTO

ACLOCAL_AMFLAGS = -I m4

SUBDIRS = chance common magic item character combat map graphics sound engine net client test_u

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = Yarp-${YARP_API_VERSION}.pc

EXTRA_DIST = scripts/Yarp.spec
