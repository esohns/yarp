########### install files ###############
install (FILES
         default_arena.level
         default_level.level
         default_player.profile
         DESTINATION ${INSTALL_SHARE_DIR} COMPONENT config)
